# expo-router declares @testing-library/react-native as a peer; auto-installing
# it pulls the RN-0.86 / test-renderer@1 tree and resolves differently across
# OSes, breaking strict `npm ci`. legacy-peer-deps skips test-only peer
# auto-install consistently on every machine, keeping the lock file stable.
legacy-peer-deps=true
